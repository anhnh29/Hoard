services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: hoard
      POSTGRES_PASSWORD: hoard
      POSTGRES_DB: hoard_dev
    ports:
      - '5434:5432'
    volumes:
      - hoard_db_data:/var/lib/postgresql/data

volumes:
  hoard_db_data:
